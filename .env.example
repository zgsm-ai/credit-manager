# 开发环境 API 代理目标配置（仅影响 npm run dev 的 vite 代理，不影响生产构建）
# 未配置时使用 vite.config.ts 中默认的共享测试环境地址。
#
# 使用方式：
#   1. 复制本文件为 .env.local 并按需修改（.env.local 已被 git 忽略，不会上库）
#   2. 或直接以环境变量形式传入，例如：
#      PROXY_TARGET_OIDC=http://127.0.0.1:8084 npm run dev

# 配额管理接口
# PROXY_TARGET_QUOTA=https://zgsmtest.cn:30443/

# OIDC 认证接口
# PROXY_TARGET_OIDC=https://zgsmtest.cn:30443/

# 订单管理接口
# PROXY_TARGET_ORDER=https://zgsmtest.cn:30443/

# 运营活动接口
# PROXY_TARGET_ACTIVITY=https://zgsm.sangfor.com
